// Inicializa a biblioteca AOS (Animate On Scroll)
AOS.init({
    duration: 1000, // Duração da animação em milissegundos
    once: true,     // A animação acontece apenas uma vez
    offset: 50,     // Distância do elemento para o início da animação
});
